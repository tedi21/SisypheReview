/*
 * IDiaEnumSymbolsPtrInterpreter_impl.hpp
 *
 *
 * @date 21-06-2016
 * @author Teddy DIDE
 * @version 1.00
 * Pdb Interpreter generated by gensources.
 */

#define A(str) encode<EncodingT,ansi>(str)
#define C(str) encode<ansi,EncodingT>(str)

NAMESPACE_BEGIN(interp)

template <class EncodingT>
class IDiaSymbolPtrInterpreter;

template <class EncodingT>
IDiaEnumSymbolsPtrInterpreter<EncodingT>::IDiaEnumSymbolsPtrInterpreter()
{
}

template <class EncodingT>
void IDiaEnumSymbolsPtrInterpreter<EncodingT>::tidyValue()
{
}

template <class EncodingT>
IDiaEnumSymbolsPtrInterpreter<EncodingT>::~IDiaEnumSymbolsPtrInterpreter()
{
	tidyValue();
}

template <class EncodingT>
void IDiaEnumSymbolsPtrInterpreter<EncodingT>::initValue(const IDiaEnumSymbolsPtr& object)
{
	m_object = object;
}

template <class EncodingT>
IDiaEnumSymbolsPtr& IDiaEnumSymbolsPtrInterpreter<EncodingT>::value()
{
	return m_object;
}

template <class EncodingT>
const IDiaEnumSymbolsPtr& IDiaEnumSymbolsPtrInterpreter<EncodingT>::value() const
{
	return m_object;
}

template <class EncodingT>
IDiaEnumSymbolsPtrInterpreter<EncodingT>::IDiaEnumSymbolsPtrInterpreter(const IDiaEnumSymbolsPtr& object)
{
	initValue(object);
}

template <class EncodingT>
IDiaEnumSymbolsPtrInterpreter<EncodingT>::IDiaEnumSymbolsPtrInterpreter(const IDiaEnumSymbolsPtrInterpreter<EncodingT>& rhs)
{
	initValue(rhs.value());
}

template <class EncodingT>
IDiaEnumSymbolsPtrInterpreter<EncodingT>& IDiaEnumSymbolsPtrInterpreter<EncodingT>::operator=(const IDiaEnumSymbolsPtrInterpreter<EncodingT>& rhs)
{
	setValue(rhs.value());
	return *this;
}

template <class EncodingT>
const IDiaEnumSymbolsPtr& IDiaEnumSymbolsPtrInterpreter<EncodingT>::getValue() const
{
	return value();
}

template <class EncodingT>
void IDiaEnumSymbolsPtrInterpreter<EncodingT>::setValue(IDiaEnumSymbolsPtr const& object)
{
	tidyValue();
	initValue(object);
}

template <class EncodingT>
typename EncodingT::string_t IDiaEnumSymbolsPtrInterpreter<EncodingT>::toString() const
{
	return EncodingT::EMPTY;
}

template <class EncodingT>
shared_ptr< Base<EncodingT> > IDiaEnumSymbolsPtrInterpreter<EncodingT>::clone() const
{
	return shared_ptr< Base<EncodingT> >(new IDiaEnumSymbolsPtrInterpreter<EncodingT>(*this));
}

template <class EncodingT>
typename EncodingT::string_t IDiaEnumSymbolsPtrInterpreter<EncodingT>::getClassName() const
{
	return C("IDiaEnumSymbolsPtr");
}

template <class EncodingT>
shared_ptr< Base<EncodingT> > IDiaEnumSymbolsPtrInterpreter<EncodingT>::invoke(const typename EncodingT::string_t& method, std::vector< shared_ptr< Base<EncodingT> > >& params)
{
	shared_ptr< Base<EncodingT> > obj(new Base<EncodingT>());
	ParameterArray args, ret;
	if (check_parameters_array(params, args))
	{
		if (tryInvoke(this, C("IDiaEnumSymbolsPtr"), method, args, ret))
		{
			find_parameter(ret, FACTORY_RETURN_PARAMETER, obj);
			for (size_t i = 0; i < params.size(); ++i)
			{
				find_parameter(ret, i, params[i]);
			}
		}
		else
		{
			Category* logger = &Category::getInstance(LOGNAME);
			logger->errorStream() << "Unexpected call in IDiaEnumSymbolsPtr, no method \"" << A(method) << "\" exists.";
		}
	}
	return obj;
}

// Récupère le nombre de symboles.
template <class EncodingT>
shared_ptr< Base<EncodingT> > IDiaEnumSymbolsPtrInterpreter<EncodingT>::get_Count(shared_ptr< Base<EncodingT> >& pRetVal)
{
	shared_ptr< Base<EncodingT> > res(new Numeric<EncodingT>());
	long nativePRetVal;
	if (check_numeric(pRetVal, nativePRetVal))
	{
		res.reset(new Numeric<EncodingT>(value()->get_Count(&nativePRetVal)));
		reset_numeric(pRetVal, nativePRetVal);
	}
	return res;
}

// Récupère un symbole au moyen d'un index.
template <class EncodingT>
shared_ptr< Base<EncodingT> > IDiaEnumSymbolsPtrInterpreter<EncodingT>::item(const shared_ptr< Base<EncodingT> >& index, shared_ptr< Base<EncodingT> >& symbol)
{
	shared_ptr< Base<EncodingT> > res(new Numeric<EncodingT>());
	long nativeIndex;
	IDiaSymbolPtr nativeSymbol;
	if (check_numeric(index, nativeIndex) && 
		check_IDiaSymbolPtr(symbol, nativeSymbol))
	{
		res.reset(new Numeric<EncodingT>(value()->Item(nativeIndex, &nativeSymbol)));
		reset_IDiaSymbolPtr(symbol, nativeSymbol);
	}
	return res;
}

// Récupère un nombre spécifié de symboles dans la séquence d'énumération.
template <class EncodingT>
shared_ptr< Base<EncodingT> > IDiaEnumSymbolsPtrInterpreter<EncodingT>::next(const shared_ptr< Base<EncodingT> >& celt, shared_ptr< Base<EncodingT> >& rgelt, shared_ptr< Base<EncodingT> >& pceltFetched)
{
	shared_ptr< Base<EncodingT> > res(new Numeric<EncodingT>());
	unsigned long nativeCelt;
	IDiaSymbolPtr nativeRgelt;
	unsigned long nativePceltFetched;
	if (check_numeric(celt, nativeCelt) && 
		check_IDiaSymbolPtr(rgelt, nativeRgelt) && 
		check_numeric(pceltFetched, nativePceltFetched))
	{
		res.reset(new Numeric<EncodingT>(value()->Next(nativeCelt, &nativeRgelt, &nativePceltFetched)));
		reset_IDiaSymbolPtr(rgelt, nativeRgelt);
		reset_numeric(pceltFetched, nativePceltFetched);
	}
	return res;
}

// Ignore un nombre spécifié de symboles dans une séquence d'énumération.
template <class EncodingT>
shared_ptr< Base<EncodingT> > IDiaEnumSymbolsPtrInterpreter<EncodingT>::skip(const shared_ptr< Base<EncodingT> >& celt)
{
	shared_ptr< Base<EncodingT> > res(new Numeric<EncodingT>());
	unsigned long nativeCelt;
	if (check_numeric(celt, nativeCelt))
	{
		res.reset(new Numeric<EncodingT>(value()->Skip(nativeCelt)));
	}
	return res;
}

// Réinitialise une séquence d'énumération au début.
template <class EncodingT>
shared_ptr< Base<EncodingT> > IDiaEnumSymbolsPtrInterpreter<EncodingT>::reset()
{
	shared_ptr< Base<EncodingT> > res(new Numeric<EncodingT>());
	res.reset(new Numeric<EncodingT>(value()->Reset()));
	return res;
}


template <class EncodingT>
bool check_IDiaEnumSymbolsPtr(shared_ptr< Base<EncodingT> > const& val, IDiaEnumSymbolsPtr& a)
{
	shared_ptr< IDiaEnumSymbolsPtrInterpreter<EncodingT> > value  = dynamic_pointer_cast< IDiaEnumSymbolsPtrInterpreter<EncodingT> >(val);
	if (value)
	{
		a = value->getValue();
	}
	else
	{
		Category * logger = &Category::getInstance(LOGNAME);
		logger->errorStream() << "IDiaEnumSymbolsPtr expected, got " << A(val->getClassName());
	}
	return value;
}

template <class EncodingT>
bool reset_IDiaEnumSymbolsPtr(shared_ptr< Base<EncodingT> >& val, IDiaEnumSymbolsPtr const& a)
{
	shared_ptr< IDiaEnumSymbolsPtrInterpreter<EncodingT> > value  = dynamic_pointer_cast< IDiaEnumSymbolsPtrInterpreter<EncodingT> >(val);
	if (value)
	{
		value->setValue(a);
	}
	else
	{
		Category* logger = &Category::getInstance(LOGNAME);
		logger->errorStream() << "IDiaEnumSymbolsPtr expected, got " << A(val->getClassName());
	}
	return value;
}

NAMESPACE_END

#undef A
#undef C