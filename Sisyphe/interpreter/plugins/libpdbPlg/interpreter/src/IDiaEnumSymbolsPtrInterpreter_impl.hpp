/*
 * IDiaEnumSymbolsPtrInterpreter_impl.hpp
 *
 *
 * @date 12-07-2020
 * @author Teddy DIDE
 * @version 1.00
 * Pdb Interpreter generated by gensources.
 */

#define A(str) encode<EncodingT,ansi>(str)
#define C(str) encode<ansi,EncodingT>(str)

NAMESPACE_BEGIN(interp)

template <class EncodingT>
class IDiaSymbolPtrInterpreter;

template <class EncodingT>
IDiaEnumSymbolsPtrInterpreter<EncodingT>::IDiaEnumSymbolsPtrInterpreter()
{
}

template <class EncodingT>
void IDiaEnumSymbolsPtrInterpreter<EncodingT>::tidyValue()
{
}

template <class EncodingT>
void IDiaEnumSymbolsPtrInterpreter<EncodingT>::initValue(const IDiaEnumSymbolsPtr& object)
{
	m_object = object;
}

template <class EncodingT>
IDiaEnumSymbolsPtr& IDiaEnumSymbolsPtrInterpreter<EncodingT>::refValue()
{
	return m_object;
}

template <class EncodingT>
const IDiaEnumSymbolsPtr& IDiaEnumSymbolsPtrInterpreter<EncodingT>::refValue() const
{
	return m_object;
}

template <class EncodingT>
IDiaEnumSymbolsPtrInterpreter<EncodingT>::IDiaEnumSymbolsPtrInterpreter(const IDiaEnumSymbolsPtr& object)
{
	initValue(object);
}

template <class EncodingT>
const IDiaEnumSymbolsPtr& IDiaEnumSymbolsPtrInterpreter<EncodingT>::value() const
{
	return refValue();
}

template <class EncodingT>
void IDiaEnumSymbolsPtrInterpreter<EncodingT>::value(IDiaEnumSymbolsPtr const& object)
{
	if (&object != &value()) {
		tidyValue();
		initValue(object);
	}
}

template <class EncodingT>
typename EncodingT::string_t IDiaEnumSymbolsPtrInterpreter<EncodingT>::toString() const
{
	return EncodingT::EMPTY;
}

template <class EncodingT>
boost::shared_ptr< Base<EncodingT> > IDiaEnumSymbolsPtrInterpreter<EncodingT>::clone() const
{
	return boost::shared_ptr< Base<EncodingT> >(new IDiaEnumSymbolsPtrInterpreter<EncodingT>(*this));
}

template <class EncodingT>
typename EncodingT::string_t IDiaEnumSymbolsPtrInterpreter<EncodingT>::getClassName() const
{
	return UCS("IDiaEnumSymbolsPtr");
}

template <class EncodingT>
boost::shared_ptr< Base<EncodingT> > IDiaEnumSymbolsPtrInterpreter<EncodingT>::invoke(const typename EncodingT::string_t& method, std::vector< boost::shared_ptr< Base<EncodingT> > >& params)
{
	boost::shared_ptr< Base<EncodingT> > obj(new Base<EncodingT>());
	ParameterArray args, ret;
	if (check_parameters_array(params, args))
	{
		if (tryInvoke(this, UCS("IDiaEnumSymbolsPtr"), method, args, ret) ||
			tryInvoke(this, UCS("Base"), method, args, ret))
		{
			find_parameter(ret, FACTORY_RETURN_PARAMETER, obj);
			for (size_t i = 0; i < params.size(); ++i)
			{
				find_parameter(ret, i, params[i]);
			}
		}
		else
		{
			Category* logger = &Category::getInstance(LOGNAME);
			logger->errorStream() << "Unexpected call in IDiaEnumSymbolsPtr, no method \"" << A(method) << "\" exists.";
		}
	}
	return obj;
}

// Récupère le nombre de symboles.
template <class EncodingT>
boost::shared_ptr< Base<EncodingT> > IDiaEnumSymbolsPtrInterpreter<EncodingT>::get_Count(boost::shared_ptr< Base<EncodingT> >& pRetVal)
{
	boost::shared_ptr< Numeric<EncodingT> > res(new Numeric<EncodingT>());
	IDiaEnumSymbolsPtr valuePtr = refValue();
	if (valuePtr != NULL)
	{
		long nativePRetVal;
		if (check_numeric_i(pRetVal, nativePRetVal))
		{
			res->LLvalue(valuePtr->get_Count(&nativePRetVal));
			reset_numeric_i(pRetVal, nativePRetVal);
		}
	}
	return res;
}

// Récupère un symbole au moyen d'un index.
template <class EncodingT>
boost::shared_ptr< Base<EncodingT> > IDiaEnumSymbolsPtrInterpreter<EncodingT>::item(const boost::shared_ptr< Base<EncodingT> >& index, boost::shared_ptr< Base<EncodingT> >& symbol)
{
	boost::shared_ptr< Numeric<EncodingT> > res(new Numeric<EncodingT>());
	IDiaEnumSymbolsPtr valuePtr = refValue();
	if (valuePtr != NULL)
	{
		long nativeIndex;
		IDiaSymbolPtr nativeSymbol;
		if (check_numeric_i(index, nativeIndex) && 
			check_IDiaSymbolPtr(symbol, nativeSymbol))
		{
			res->LLvalue(valuePtr->Item(nativeIndex, &nativeSymbol));
			reset_IDiaSymbolPtr(symbol, nativeSymbol);
		}
	}
	return res;
}

// Récupère un nombre spécifié de symboles dans la séquence d'énumération.
template <class EncodingT>
boost::shared_ptr< Base<EncodingT> > IDiaEnumSymbolsPtrInterpreter<EncodingT>::next(const boost::shared_ptr< Base<EncodingT> >& celt, boost::shared_ptr< Base<EncodingT> >& rgelt, boost::shared_ptr< Base<EncodingT> >& pceltFetched)
{
	boost::shared_ptr< Numeric<EncodingT> > res(new Numeric<EncodingT>());
	IDiaEnumSymbolsPtr valuePtr = refValue();
	if (valuePtr != NULL)
	{
		unsigned long nativeCelt;
		IDiaSymbolPtr nativeRgelt;
		unsigned long nativePceltFetched;
		if (check_numeric_i(celt, nativeCelt) && 
			check_IDiaSymbolPtr(rgelt, nativeRgelt) && 
			check_numeric_i(pceltFetched, nativePceltFetched))
		{
			res->LLvalue(valuePtr->Next(nativeCelt, &nativeRgelt, &nativePceltFetched));
			reset_IDiaSymbolPtr(rgelt, nativeRgelt);
			reset_numeric_i(pceltFetched, nativePceltFetched);
		}
	}
	return res;
}

// Ignore un nombre spécifié de symboles dans une séquence d'énumération.
template <class EncodingT>
boost::shared_ptr< Base<EncodingT> > IDiaEnumSymbolsPtrInterpreter<EncodingT>::skip(const boost::shared_ptr< Base<EncodingT> >& celt)
{
	boost::shared_ptr< Numeric<EncodingT> > res(new Numeric<EncodingT>());
	IDiaEnumSymbolsPtr valuePtr = refValue();
	if (valuePtr != NULL)
	{
		unsigned long nativeCelt;
		if (check_numeric_i(celt, nativeCelt))
		{
			res->LLvalue(valuePtr->Skip(nativeCelt));
		}
	}
	return res;
}

// Réinitialise une séquence d'énumération au début.
template <class EncodingT>
boost::shared_ptr< Base<EncodingT> > IDiaEnumSymbolsPtrInterpreter<EncodingT>::reset()
{
	boost::shared_ptr< Numeric<EncodingT> > res(new Numeric<EncodingT>());
	IDiaEnumSymbolsPtr valuePtr = refValue();
	if (valuePtr != NULL)
	{
		res->LLvalue(valuePtr->Reset());
	}
	return res;
}


template <class EncodingT>
bool check_IDiaEnumSymbolsPtr(boost::shared_ptr< Base<EncodingT> > const& val, IDiaEnumSymbolsPtr& a)
{
	boost::shared_ptr< IDiaEnumSymbolsPtrInterpreter<EncodingT> > value  = dynamic_pointer_cast< IDiaEnumSymbolsPtrInterpreter<EncodingT> >(val);
	if (value)
	{
		a = value->value();
	}
	else
	{
		Category * logger = &Category::getInstance(LOGNAME);
		logger->errorStream() << "IDiaEnumSymbolsPtr expected, got " << A(val->getClassName());
	}
	return (value != NULL);
}

template <class EncodingT>
bool reset_IDiaEnumSymbolsPtr(boost::shared_ptr< Base<EncodingT> >& val, IDiaEnumSymbolsPtr const& a)
{
	boost::shared_ptr< IDiaEnumSymbolsPtrInterpreter<EncodingT> > value  = dynamic_pointer_cast< IDiaEnumSymbolsPtrInterpreter<EncodingT> >(val);
	if (value)
	{
		value->value(a);
	}
	else
	{
		Category* logger = &Category::getInstance(LOGNAME);
		logger->errorStream() << "IDiaEnumSymbolsPtr expected, got " << A(val->getClassName());
	}
	return (value != NULL);
}

NAMESPACE_END

#undef A
#undef C