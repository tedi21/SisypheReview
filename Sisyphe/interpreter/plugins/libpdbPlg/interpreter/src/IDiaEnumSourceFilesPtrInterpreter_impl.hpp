/*
 * IDiaEnumSourceFilesPtrInterpreter_impl.hpp
 *
 *
 * @date 12-07-2020
 * @author Teddy DIDE
 * @version 1.00
 * Pdb Interpreter generated by gensources.
 */

#define A(str) encode<EncodingT,ansi>(str)
#define C(str) encode<ansi,EncodingT>(str)

NAMESPACE_BEGIN(interp)

template <class EncodingT>
class IDiaSourceFilePtrInterpreter;

template <class EncodingT>
IDiaEnumSourceFilesPtrInterpreter<EncodingT>::IDiaEnumSourceFilesPtrInterpreter()
{
}

template <class EncodingT>
void IDiaEnumSourceFilesPtrInterpreter<EncodingT>::tidyValue()
{
}

template <class EncodingT>
void IDiaEnumSourceFilesPtrInterpreter<EncodingT>::initValue(const IDiaEnumSourceFilesPtr& object)
{
	m_object = object;
}

template <class EncodingT>
IDiaEnumSourceFilesPtr& IDiaEnumSourceFilesPtrInterpreter<EncodingT>::refValue()
{
	return m_object;
}

template <class EncodingT>
const IDiaEnumSourceFilesPtr& IDiaEnumSourceFilesPtrInterpreter<EncodingT>::refValue() const
{
	return m_object;
}

template <class EncodingT>
IDiaEnumSourceFilesPtrInterpreter<EncodingT>::IDiaEnumSourceFilesPtrInterpreter(const IDiaEnumSourceFilesPtr& object)
{
	initValue(object);
}

template <class EncodingT>
const IDiaEnumSourceFilesPtr& IDiaEnumSourceFilesPtrInterpreter<EncodingT>::value() const
{
	return refValue();
}

template <class EncodingT>
void IDiaEnumSourceFilesPtrInterpreter<EncodingT>::value(IDiaEnumSourceFilesPtr const& object)
{
	if (&object != &value()) {
		tidyValue();
		initValue(object);
	}
}

template <class EncodingT>
typename EncodingT::string_t IDiaEnumSourceFilesPtrInterpreter<EncodingT>::toString() const
{
	return EncodingT::EMPTY;
}

template <class EncodingT>
boost::shared_ptr< Base<EncodingT> > IDiaEnumSourceFilesPtrInterpreter<EncodingT>::clone() const
{
	return boost::shared_ptr< Base<EncodingT> >(new IDiaEnumSourceFilesPtrInterpreter<EncodingT>(*this));
}

template <class EncodingT>
typename EncodingT::string_t IDiaEnumSourceFilesPtrInterpreter<EncodingT>::getClassName() const
{
	return UCS("IDiaEnumSourceFilesPtr");
}

template <class EncodingT>
boost::shared_ptr< Base<EncodingT> > IDiaEnumSourceFilesPtrInterpreter<EncodingT>::invoke(const typename EncodingT::string_t& method, std::vector< boost::shared_ptr< Base<EncodingT> > >& params)
{
	boost::shared_ptr< Base<EncodingT> > obj(new Base<EncodingT>());
	ParameterArray args, ret;
	if (check_parameters_array(params, args))
	{
		if (tryInvoke(this, UCS("IDiaEnumSourceFilesPtr"), method, args, ret) ||
			tryInvoke(this, UCS("Base"), method, args, ret))
		{
			find_parameter(ret, FACTORY_RETURN_PARAMETER, obj);
			for (size_t i = 0; i < params.size(); ++i)
			{
				find_parameter(ret, i, params[i]);
			}
		}
		else
		{
			Category* logger = &Category::getInstance(LOGNAME);
			logger->errorStream() << "Unexpected call in IDiaEnumSourceFilesPtr, no method \"" << A(method) << "\" exists.";
		}
	}
	return obj;
}

// Récupère le nombre de fichiers sources.
template <class EncodingT>
boost::shared_ptr< Base<EncodingT> > IDiaEnumSourceFilesPtrInterpreter<EncodingT>::get_Count(boost::shared_ptr< Base<EncodingT> >& pRetVal)
{
	boost::shared_ptr< Numeric<EncodingT> > res(new Numeric<EncodingT>());
	IDiaEnumSourceFilesPtr valuePtr = refValue();
	if (valuePtr != NULL)
	{
		long nativePRetVal;
		if (check_numeric_i(pRetVal, nativePRetVal))
		{
			res->LLvalue(valuePtr->get_Count(&nativePRetVal));
			reset_numeric_i(pRetVal, nativePRetVal);
		}
	}
	return res;
}

// Extrait un fichier source au moyen d'un index.
template <class EncodingT>
boost::shared_ptr< Base<EncodingT> > IDiaEnumSourceFilesPtrInterpreter<EncodingT>::item(const boost::shared_ptr< Base<EncodingT> >& index, boost::shared_ptr< Base<EncodingT> >& symbol)
{
	boost::shared_ptr< Numeric<EncodingT> > res(new Numeric<EncodingT>());
	IDiaEnumSourceFilesPtr valuePtr = refValue();
	if (valuePtr != NULL)
	{
		long nativeIndex;
		IDiaSourceFilePtr nativeSymbol;
		if (check_numeric_i(index, nativeIndex) && 
			check_IDiaSourceFilePtr(symbol, nativeSymbol))
		{
			res->LLvalue(valuePtr->Item(nativeIndex, &nativeSymbol));
			reset_IDiaSourceFilePtr(symbol, nativeSymbol);
		}
	}
	return res;
}

// Récupère un nombre spécifié de fichiers sources dans la séquence d'énumération.
template <class EncodingT>
boost::shared_ptr< Base<EncodingT> > IDiaEnumSourceFilesPtrInterpreter<EncodingT>::next(const boost::shared_ptr< Base<EncodingT> >& celt, boost::shared_ptr< Base<EncodingT> >& rgelt, boost::shared_ptr< Base<EncodingT> >& pceltFetched)
{
	boost::shared_ptr< Numeric<EncodingT> > res(new Numeric<EncodingT>());
	IDiaEnumSourceFilesPtr valuePtr = refValue();
	if (valuePtr != NULL)
	{
		unsigned long nativeCelt;
		IDiaSourceFilePtr nativeRgelt;
		unsigned long nativePceltFetched;
		if (check_numeric_i(celt, nativeCelt) && 
			check_IDiaSourceFilePtr(rgelt, nativeRgelt) && 
			check_numeric_i(pceltFetched, nativePceltFetched))
		{
			res->LLvalue(valuePtr->Next(nativeCelt, &nativeRgelt, &nativePceltFetched));
			reset_IDiaSourceFilePtr(rgelt, nativeRgelt);
			reset_numeric_i(pceltFetched, nativePceltFetched);
		}
	}
	return res;
}

// Ignore un nombre spécifié de fichiers sources dans une séquence d'énumération.
template <class EncodingT>
boost::shared_ptr< Base<EncodingT> > IDiaEnumSourceFilesPtrInterpreter<EncodingT>::skip(const boost::shared_ptr< Base<EncodingT> >& celt)
{
	boost::shared_ptr< Numeric<EncodingT> > res(new Numeric<EncodingT>());
	IDiaEnumSourceFilesPtr valuePtr = refValue();
	if (valuePtr != NULL)
	{
		unsigned long nativeCelt;
		if (check_numeric_i(celt, nativeCelt))
		{
			res->LLvalue(valuePtr->Skip(nativeCelt));
		}
	}
	return res;
}

// Réinitialise une séquence d'énumération au début.
template <class EncodingT>
boost::shared_ptr< Base<EncodingT> > IDiaEnumSourceFilesPtrInterpreter<EncodingT>::reset()
{
	boost::shared_ptr< Numeric<EncodingT> > res(new Numeric<EncodingT>());
	IDiaEnumSourceFilesPtr valuePtr = refValue();
	if (valuePtr != NULL)
	{
		res->LLvalue(valuePtr->Reset());
	}
	return res;
}


template <class EncodingT>
bool check_IDiaEnumSourceFilesPtr(boost::shared_ptr< Base<EncodingT> > const& val, IDiaEnumSourceFilesPtr& a)
{
	boost::shared_ptr< IDiaEnumSourceFilesPtrInterpreter<EncodingT> > value  = dynamic_pointer_cast< IDiaEnumSourceFilesPtrInterpreter<EncodingT> >(val);
	if (value)
	{
		a = value->value();
	}
	else
	{
		Category * logger = &Category::getInstance(LOGNAME);
		logger->errorStream() << "IDiaEnumSourceFilesPtr expected, got " << A(val->getClassName());
	}
	return (value != NULL);
}

template <class EncodingT>
bool reset_IDiaEnumSourceFilesPtr(boost::shared_ptr< Base<EncodingT> >& val, IDiaEnumSourceFilesPtr const& a)
{
	boost::shared_ptr< IDiaEnumSourceFilesPtrInterpreter<EncodingT> > value  = dynamic_pointer_cast< IDiaEnumSourceFilesPtrInterpreter<EncodingT> >(val);
	if (value)
	{
		value->value(a);
	}
	else
	{
		Category* logger = &Category::getInstance(LOGNAME);
		logger->errorStream() << "IDiaEnumSourceFilesPtr expected, got " << A(val->getClassName());
	}
	return (value != NULL);
}

NAMESPACE_END

#undef A
#undef C