/*
 * StringListIteratorInterpreter_impl.hpp
 *
 *
 * @date 29-05-2015
 * @author Teddy DIDE
 * @version 1.00
 * STL Interpreter generated by gensources.
 */

#define A(str) encode<EncodingT,ansi>(str)
#define C(str) encode<ansi,EncodingT>(str)

NAMESPACE_BEGIN(interp)

template <class EncodingT>
StringListIteratorInterpreter<EncodingT>::StringListIteratorInterpreter()
{}

template <class EncodingT>
StringListIteratorInterpreter<EncodingT>::~StringListIteratorInterpreter()
{}

template <class EncodingT>
StringListIteratorInterpreter<EncodingT>::StringListIteratorInterpreter(const StringListIterator<EncodingT>& object)
: m_object(object)
{}

template <class EncodingT>
const StringListIterator<EncodingT>& StringListIteratorInterpreter<EncodingT>::value() const
{
	return m_object;
}

template <class EncodingT>
void StringListIteratorInterpreter<EncodingT>::value(StringListIterator<EncodingT> const& object)
{
	m_object = object;
}

template <class EncodingT>
typename EncodingT::string_t StringListIteratorInterpreter<EncodingT>::toString() const
{
    return EncodingT::EMPTY;
}

template <class EncodingT>
boost::shared_ptr< Base<EncodingT> > StringListIteratorInterpreter<EncodingT>::clone() const
{
        return boost::shared_ptr< Base<EncodingT> >(new StringListIteratorInterpreter<EncodingT>());
}

template <class EncodingT>
typename EncodingT::string_t StringListIteratorInterpreter<EncodingT>::getClassName() const
{
        return UCS("StringListIterator");
}

template <class EncodingT>
boost::shared_ptr< Base<EncodingT> > StringListIteratorInterpreter<EncodingT>::invoke(const typename EncodingT::string_t& method, std::vector< boost::shared_ptr< Base<EncodingT> > >& params)
{
        boost::shared_ptr< Base<EncodingT> > obj(new Base<EncodingT>());
	ParameterArray args, ret;
	if (check_parameters_array(params, args))
	{
                if (tryInvoke(this, UCS("StringListIterator"), method, args, ret) ||
                    tryInvoke(this, UCS("Base"), method, args, ret))
		{
			find_parameter(ret, FACTORY_RETURN_PARAMETER, obj);
			for (size_t i = 0; i < params.size(); ++i)
			{
				find_parameter(ret, i, params[i]);
			}
		}
		else
		{
			Category* logger = &Category::getInstance(LOGNAME);
			logger->errorStream() << "Unexpected call in StringListIterator, no method \"" << A(method) << "\" exists.";
		}
	}
	return obj;
}

// Retourne la valeur courante
template <class EncodingT>
boost::shared_ptr< Base<EncodingT> > StringListIteratorInterpreter<EncodingT>::current() const
{
        return boost::shared_ptr< Base<EncodingT> > (new String<EncodingT>(*m_object));
}

// Retourne l'itérateur du début
template <class EncodingT>
void StringListIteratorInterpreter<EncodingT>::increment()
{
    ++m_object;
}

// Retourne l'itérateur de fin
template <class EncodingT>
void StringListIteratorInterpreter<EncodingT>::decrement()
{
    --m_object;
}

template <class EncodingT>
boost::shared_ptr< Base<EncodingT> > StringListIteratorInterpreter<EncodingT>::equals(boost::shared_ptr< Base<EncodingT> > const& val) const
{
    boost::shared_ptr< Base<EncodingT> > res(new Bool<EncodingT>);
    StringListIterator<EncodingT> ite;
    if (check_StringListIterator(val, ite))
    {
        res.reset(new Bool<EncodingT>(m_object == ite));
    }
    return res;
}

template <class EncodingT>
boost::shared_ptr< Base<EncodingT> > StringListIteratorInterpreter<EncodingT>::notEquals(boost::shared_ptr< Base<EncodingT> > const& val) const
{
    boost::shared_ptr< Base<EncodingT> > res(new Bool<EncodingT>);
    StringListIterator<EncodingT> ite;
    if (check_StringListIterator(val, ite))
    {
        res.reset(new Bool<EncodingT>(m_object != ite));
    }
    return res;
}

template <class EncodingT>
bool check_StringListIterator(boost::shared_ptr< Base<EncodingT> > const& val, StringListIterator<EncodingT>& a)
{
  boost::shared_ptr< StringListIteratorInterpreter<EncodingT> > value  = dynamic_pointer_cast< StringListIteratorInterpreter<EncodingT> >(val);
  if (value)
  {
    a = value->value();
  }
  else
  {
    Category * logger = &Category::getInstance(LOGNAME);
    logger->errorStream() << "StringListIterator expected, got " << A(val->getClassName());
  }
  return (value != NULL);
}

template <class EncodingT>
bool reset_StringListIterator(boost::shared_ptr< Base<EncodingT> >& val, StringListIterator<EncodingT> const& a)
{
  boost::shared_ptr< StringListIteratorInterpreter<EncodingT> > value  = dynamic_pointer_cast< StringListIteratorInterpreter<EncodingT> >(val);
  if (value)
  {
    value->value(a);
  }
  else
  {
    Category* logger = &Category::getInstance(LOGNAME);
    logger->errorStream() << "StringListIterator expected, got " << A(val->getClassName());
  }
  return (value != NULL);
}

NAMESPACE_END

#undef A
#undef C
